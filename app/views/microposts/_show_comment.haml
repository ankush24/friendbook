
- comments.each do |comment|
  %li
    %span.user= link_to comment.user.email, posts_show_path(comment.micropost.id)
    %br/
    %span.content
      = comment.comments
  %br/
  %br/
  %span.timestamp
    Posted #{time_ago_in_words(comment.created_at)} ago.
  %hr